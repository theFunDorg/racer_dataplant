/.z.pc
/.z.po
/.ipc.hopen



// TODO: Adding the whole file.This file will do the following:
// * .z.po table of functions to call upon opening handle
// * .z.pc table of functions to call upon opening handle
// * define .z.po/pc to pass the handle to each of these handles
// * create open/close functions that are added to the above tables. 
// * create table of handles with login/out details
// * Define the hopen function to add handle to a table, with open time